Metadata-Version: 2.4
Name: claw-jijin-dogami
Version: 0.1.0
Summary: Fund analysis service for clawdbot integration
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: fastapi<1.0,>=0.115
Requires-Dist: uvicorn<1.0,>=0.30
Requires-Dist: pydantic<3.0,>=2.7
Provides-Extra: dev
Requires-Dist: httpx<1.0,>=0.27; extra == "dev"
Requires-Dist: pytest<9.0,>=8.0; extra == "dev"

# claw-jijin-dogami

一个面向**个人投资者**的国内基金 AI 辅助分析项目，目标是围绕 **T+1 场外基金** 做三件事：

- 把真实持仓、收益、费率、回撤、仓位等数据算清楚
- 把重大消息、行业变化、宏观信号和你的持仓关联起来
- 在**可检验、可回放、可评分**的前提下，给出结构化投资建议

> 核心原则：**规则和数据负责算准，AI 负责解释、归因、建议。**

## 文档导航

- 架构与集成设计：[`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)
- 开发流程与阶段规划：[`docs/DEVELOPMENT-FLOW.md`](docs/DEVELOPMENT-FLOW.md)

## 1. 项目定位

这个项目不是“会聊天的荐基机器人”，而是一个更接近个人投研助手的系统：

- 面向中国个人投资者
- 主要服务国内公募基金、指数基金、ETF 联接基金等常见品类
- 聚焦场外基金的 T+1 使用场景
- 强调历史回放验证，而不是只看当下对话效果

## 2. 预想使用场景

当前预期的核心场景如下：

### 2.1 日常看盘 / 复盘

用户每天希望快速知道：

- 今日整体持仓收益如何
- 哪些基金拖累或贡献了组合收益
- 当前仓位是否过于集中
- 哪些基金近期风险上升
- 今天有没有值得重点关注的政策、行业或公司消息

### 2.2 重大消息影响分析

用户输入一条新闻、公告、政策或市场事件，希望系统回答：

- 这条消息影响我的哪些基金
- 影响方向偏正面还是偏负面
- 更偏短期扰动还是中期趋势
- 哪些判断来自公开持仓穿透，哪些只是低置信推断

### 2.3 操作前辅助决策

用户在准备申购、赎回、定投、换基前，希望系统辅助判断：

- 当前这只基金是否还适合继续持有
- 是否值得加仓 / 暂停定投 / 分批减仓
- 如果要换基，候选基金风格是否和现有持仓重叠过高
- 动作背后的理由和风险是什么

### 2.4 历史截断回放评测

用户希望在某个历史时点复盘：

- “如果只给系统截至 2025-06-30 20:00 能看到的信息，它会怎么分析？”
- “它当时会给出什么建议？”
- “之后 5 / 20 / 60 个交易日的真实结果如何？”

这个能力用于：

- 检验 AI 分析能力
- 检验建议质量
- 检验整套系统是否真正可用

## 3. 项目目标

### 3.1 要解决的问题

个人基金投资常见痛点：

- 多只基金分散持有后，组合层面不容易统一看清
- 消息多，但很难判断到底影响了自己哪只基金
- 主动基金持仓披露滞后，容易产生“感觉正确、实际失真”的分析
- AI 很容易说得像，但很难验证它当时是不是“真的能用”

### 3.2 本项目的目标

- 建立一个**真实持仓驱动**的基金分析系统
- 建立一个**点位截断**的历史回放评测体系
- 让 AI 输出从自然语言升级为**结构化建议**
- 让结果最终能被：
  - 数据验证
  - 回测验证
  - 基准策略验证

## 4. 方案总览

推荐采用“数据层 + 规则层 + AI 层 + 评测层”的组合式架构。

当前明确采用 **方案 A：基金系统作为独立服务，`clawdbot` 作为多入口会话与调度层。**

| 层级 | 组件 | 作用 |
| --- | --- | --- |
| 持仓与指标层 | `xalpha` | 管理真实持仓、收益率、XIRR、回撤、仓位、组合分析、简单回测 |
| 国内数据层 | `AKShare` | 基金净值、费率、持仓、指数、宏观、新闻等公共数据 |
| API 封装层 | `AKTools` | 将 AKShare 快速暴露为 HTTP API，方便 AI 或前端调用 |
| 补充数据层 | `efinance` | 作为基金信息、历史净值、公开持仓的免费补充源 |
| 存储层 | `DuckDB + Parquet` | 存历史快照、新闻、基金基础信息、评测结果 |
| 规则引擎层 | 自定义规则 | 算暴露、消息影响分、仓位上限、风格重合、风险阈值 |
| AI 层 | LLM / Agent | 解释数据、做归因、输出结构化建议、生成日报/周报 |
| 评测层 | Walk-forward 回放 | 按历史时点截断数据，检验分析与策略是否有效 |

## 5. 推荐技术组合

### 5.1 为什么推荐这套组合

#### `xalpha`

适合作为本项目的“组合账本与指标核心”：

- 适合基金投资场景
- 能管理申购、赎回、定投、组合分析
- 可以把收益率、成本、回撤等关键数据算准
- 天然适合作为 AI 的“标准答案来源”

#### `AKShare`

适合作为国内公开数据底座：

- 覆盖基金、指数、行业、宏观、新闻等公共信息
- 免费、生态成熟、资料多
- 很适合做事件驱动分析和回测数据准备

#### `AKTools`

适合作为数据服务层：

- 把 Python 数据抓取快速变成 HTTP API
- 方便 AI 系统、前端、调度任务统一调用
- 降低后续系统集成难度

#### `efinance`

适合作为补充和兜底：

- 免费
- 上手快
- 对基金公开持仓、基础信息、历史数据有补充价值

#### AI 层

AI 在本项目里不负责“裸算数”，主要负责：

- 解释指标
- 解释风险
- 解释消息为什么影响持仓
- 输出结构化建议
- 生成复盘与报告

## 6. 核心功能设计

### 6.1 持仓台账与组合画像

系统需要支持：

- 基金买入 / 卖出 / 定投 / 分红记录
- 当前持仓、成本、收益、累计收益
- 组合仓位分布
- 单基金与组合层最大回撤
- 波动率、Sharpe、Sortino、Calmar 等基础指标

### 6.2 基金穿透分析

系统需要支持：

- 基金历史净值与累计净值
- 基金费率、同类排名、排名百分位
- 基金公开持仓穿透
- 底层重仓股与行业暴露
- 持仓之间的风格重叠与集中度评估

### 6.3 消息影响分析

系统需要支持：

- 新闻 / 公告 / 政策 / 宏观事件录入
- 事件标签化：公司、行业、宏观、政策、主题
- 将事件映射到基金持仓暴露
- 输出：
  - 受影响基金列表
  - 影响方向
  - 影响强度
  - 短中期倾向
  - 置信度

### 6.4 结构化建议输出

建议输出不应该只是自然语言，而应包含：

- `fund_code`
- `action`：`buy / hold / reduce / avoid / switch`
- `score`
- `confidence`
- `reasons`
- `risk_flags`
- `portfolio_action`

这样才能被后续回测和评估模块消费。

### 6.5 历史回放评测

系统必须支持：

- 选择历史截断时点 `T`
- 仅使用 `T` 时点前公开可得的数据
- 让系统在该时点输出分析与建议
- 用 `T` 之后的真实行情和结果进行评分

## 7. 使用流程图

下面是预期的主使用流程。

```mermaid
flowchart TD
    A[用户导入基金持仓与交易记录] --> B[持仓账本与组合指标计算]
    B --> C[拉取基金净值/持仓/费率/宏观/新闻]
    C --> D[规则层计算暴露、风险、影响分]
    D --> E{用户当前需求}
    E -->|日常复盘| F[生成持仓日报/周报]
    E -->|重大消息分析| G[定位受影响基金与组合]
    E -->|操作前咨询| H[输出结构化建议]
    F --> I[AI 解释结论与关键风险]
    G --> I
    H --> I
    I --> J[用户查看建议、理由、置信度]
```

## 8. 历史评测流程图

下面是系统可检验性的核心流程。

```mermaid
flowchart TD
    A[选择历史时点 T] --> B[冻结 T 时点前可见数据]
    B --> C[构建当时的持仓/净值/持仓穿透/新闻快照]
    C --> D[规则层与 AI 在 T 时点输出分析]
    D --> E[记录结构化建议与置信度]
    E --> F[对比 T 之后 5/20/60 交易日真实结果]
    F --> G[计算方向准确率/超额收益/回撤/稳定性]
    G --> H[与基准策略对比]
    H --> I[沉淀评测报告与优化结论]
```

## 9. 预期用户使用流程

### 9.1 初始化

用户首次使用时：

1. 导入基金交易记录
2. 生成初始持仓账本
3. 同步基金基础信息和公开持仓
4. 建立本地历史数据仓

### 9.2 每日使用

用户日常使用时：

1. 系统更新基金净值、新闻和市场数据
2. 系统输出组合日报
3. 用户查看：
   - 今日变化
   - 风险提示
   - 重大消息影响
   - AI 建议

### 9.3 事件驱动使用

用户输入新闻或问题：

1. 输入事件或直接贴新闻
2. 系统做事件解析与持仓映射
3. 规则层计算影响
4. AI 解释为什么影响当前持仓
5. 输出“建议 + 风险 + 置信度”

### 9.4 历史验证使用

用户做回放时：

1. 指定历史时点
2. 系统重建当时视角
3. 生成当时建议
4. 对比未来真实结果
5. 输出评分和策略表现

## 10. MVP 范围

第一阶段先做最小可用版本，不追求一次做全：

### Phase 1：可用基础版

- 持仓导入
- 基金净值更新
- 组合收益与风险指标
- 基金持仓穿透
- 单条消息影响分析
- AI 日报 / 解释 / 结构化建议

### Phase 2：评测版

- 历史截断快照
- Walk-forward 回放
- 与基准策略对比
- 方向准确率与收益表现评分

### Phase 3：增强版

- 多基金组合调仓建议
- 风格漂移识别
- 消息聚类与主题跟踪
- 周报 / 月报 / 长周期复盘

## 11. 非目标

当前版本不以以下目标为优先：

- 自动实盘交易
- 高频策略
- 高频盘中估值的绝对精确预测
- 完全依赖 AI 自由发挥做买卖决策

## 12. 一句话总结

`claw-jijin-dogami` 的目标是做一个：

> **围绕国内基金个人投资场景，能算清、能解释、能回放、能验证的 AI 辅助分析系统。**
